#include <iostream>
using namespace std;
/*
int main() {
	int first;
	int second;

	cin >> first >> second;

	if (first > second)
		cout << "The first integer is greater than the second\n";
	else if (first < second)
		cout << "The second integer is greater than the first\n";
	else
		cout << "Two integer is the same";

	return 0;
}

int main() {
	int x = 6;
	switch (x) {
	case 1: 
		cout << "x is 1\n";
		break;
	case 2:
	case 3:
		cout << "x is 2 or 3\n";
		break;
	default:
		cout << "x is not 1, 2, or 3\n";
	}
	return 0;
}

int main() {
	int x = 0;
	while (x < 10)
		x = x + 1;
	cout << "x is " << x << "\n";
	return 0;
}

int main() {
	for (int x = 0; x < 10; x = x + 1)
		cout << x << "\n";
	return 0;
}

int main() {
	int N;
	cout << "Enter N: ";
	cin >> N;

	int acc = 0;

	cin >> acc;
	int minVal = acc;
	int maxVal = acc;

	for (int i = 1; i < N; i = i + 1) {
		int a;
		cin >> a;
		acc = acc + a;
		if (a < minVal) {
			minVal = a;
		}
		if (a > maxVal) {
			maxVal = a;
		}
	}

	cout << "Mean: " << (double)acc / N << "\n";
	cout << "Max: " << maxVal << "\n";
	cout << "Min: " << minVal << "\n";

	return 0;
}

int main() {
	int N;
	cin >> N;
	for (int i = 2; N > 0; i = i + 1) {
		bool isPrime = true;
		for (int j = 2; j < i; j = j + 1) {
			if (i % j == 0) {
				isPrime = false;
				break;
			}
		}
		if (isPrime) {
			N = N - 1;
			cout << i << "\n";
		}
	}
	return 0;
}
*/